{
  "name": "maison-store",
  "version": "1.0.0",
  "private": true,
  "description": "Maison Store static storefront with a local MySQL store backend.",
  "scripts": {
    "start": "node server.js",
    "init-db": "node server.js --init-db"
  }
}
